<div id="template_local_context_data"><!--
  <div>
    <h4>${label.name}</h4>
    <p>
      <img class="local-context-image" src="${label.img_url}" />
      <span class="local-context-description">
        ${label.default_text}
        <span class="local-context-placed-by">
          <span class="local-context-placed-by-label">Placed by:</span> ${label.community}
        </span>
      </span>
    </p>
  </div>
--></div>

<div id="template_local_context_error"><!--
  <div>
    <h4>Problem Fetching Data</h4>
    <p>
      An error occurred while fetching the data from the Local Contexts Hub for project id: ${id}. Try again or consult the logs.
    </p>
  </div>
--></div>
