<%= render(:layout => '/shared/sidebar',
           :locals => {
             :record_type => 'local_contexts_project',
             :record => @local_contexts_project,
             :plusone => false,
             :suppress_basic_information => false
           }) do |sidebar| %>
<% end %>
